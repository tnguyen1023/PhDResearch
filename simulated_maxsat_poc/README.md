# MaxSAT vs Baseline Honeypot Optimization

This project compares MaxSAT-based optimization with baseline strategies for honeypot deployment in a simulated network. It generates comprehensive charts and metrics to demonstrate the effectiveness of each approach.

## Requirements

- Python 3.12
- pip

## Install Dependencies

```bash
pip install -r requirements.txt

# Run the Comparison Script
```bash
python3 ./simulated_maxsat_more_comparisions_best5_pysat_parallel_poc4.py


